# Session Handoff — 2026-06-23

Everything below is **committed and pushed to `origin/main`** (commits `63e07f9 → 3b7edc0`).
Read with `CLAUDE.md` (live state + the non-negotiable engineering principle), `PIPELINE_ARCHITECTURE.md`
(stages incl. S3/S4), and the memory files.

**This session closed out the dashboard/data-architecture front. The next session pivots to LinkedIn
content for site engagement** (see "Next focus" below).

---

## What this session shipped (in order, all pushed)

1. **Payments YoY-trend insights + a real YoY chart mode** (`baa2337`). 5 YoY-trajectory rules; payments
   chart gained a "YoY %" radio + YoY line (parity with SIBC), x-axis trimmed of the leading null year.

2. **YoY sourced from the registry, not recomputed** (`baa2337`). YoY is a *registered* L1 signal
   (`*-yoy`, `csv_total_yoy`) in `signals.db`. The dashboard now READS those values (`load_db_yoy`) instead
   of a parallel client/Stage-4a recompute. Stage 4c hard-fails on any drift (`check_yoy_matches_db`).

3. **LLM-representation alignment — payments now mirrors SIBC** (`baa2337`, `0011ea7`). Stage 4b keeps
   deterministic *selection + UI routing + `basis.facts`*, but **overrides the prose of anchored scalar
   insights with the LLM eval narrative** (prompt v1.11, via the API). `EVAL_ANCHOR` maps insight → registered
   signal. Scan/share/concentration/composed stay deterministic — the SIBC scalar=LLM / scan=deterministic
   split. **Spec expanded** with 6 new L1 signals (streaks + a ratio) → **11 payments cards LLM-represented**.

4. **L2 deterministic alignment — Check 4f STRICT in both gates** (`e51f9e6`). L2 findings = signals mapped
   onto the system model (NOT LLM independent inference). `derive_opportunities` emits `evidence_all` (driver's
   full declared signal set) so structural risks ground even when not firing; Check 4f validates numbers vs it.
   Added the missing `creates_risk` edge for `risk_cc_market_concentration`; fixed the `FY22-24`→`-24` regex
   bug (shared with Check 2g); `generate_opportunity_narrative` now uses the API (`evaluate._call_llm`).

5. **L2 content alignment — registry is L1-computed-only-ACTIVE on both pipelines** (`9b7a03a`). Payments'
   3 policy forces were already sourced (re-verified RBI/PIB) — added their `description`. SIBC's 34 registry
   layer-2 inference signals were **inert tracker stubs**; per-item verified against the model (~19 already
   captured, ~12 are L1 sub-sector trends, 2 genuine data gaps) → **added 2 gap nodes**, **retired all 34**
   (`migrated_to` pointer). Both pipelines now derive L2 solely from `system_model.json` → S3 → opportunities.

6. **Docs: S4 + L1/L2 ingestion stages** (`4b24afd`). PIPELINE_ARCHITECTURE.md gained Stage 5.7 (L2 dynamic
   ingestion) and Stage 8 (S4 inference loop); clarified Stage 5 is L1 LLM-interpretation, not L2.

7. **Payments perf — precomputed compact chart series** (`d128f8f`). `/payments` was downloading the 4.6 MB /
   47k-row CSV and re-deriving every series client-side. New generic `generate_chart_series.py` →
   `atm_pos_chart_series.json` (330 KB, numbers-only); the web does O(1) lookups, no PapaParse. ~84 ms vs
   ~270 ms. **Enshrined the engineering principle** (design for long term / compute-once-ship-compact) in
   CLAUDE.md + memory [[feedback-design-for-long-term]].

8. **SIBC aggregate "Total" per card, off by default** (`3b7edc0`). The Total is the section's **official
   parent total row** from the source (Bank Credit `I`, Industry `2`, Services `3`, Personal Loans `4`) —
   NOT a client-side sum (our own gaps prove sub-series don't reconcile). Shown-but-unselected so sub-series
   keep scale (`TrendChart` `initialHidden`); excluded from the % distribution. Applied to 4 cards; Industry
   by Type (cross-statement total) + Priority Sector (no clean parent) deliberately skipped.

**Net architecture state:** both pipelines are now **aligned by construction** — L1 deterministic compute →
LLM interpretation for scalar representation (traceable, Check 2g/4c) → L2 from the system model (S3 +
derive_opportunities, Check 4f strict) → LLM narrates only. Registry = L1-computed-only-active. Web ships
compact precomputed artifacts (no raw-CSV client parsing).

---

## Open / deferred (none blocking)

1. **S4 review backlog** — `run_inference.py --no-llm` flags **30 unexplained movements** (15 SIBC + 15
   ATM/POS) the model doesn't yet attribute a force to. Most are routine; turning any into a model force needs
   the LLM-propose step + **real sourcing** (sourcing-gated, never fabricated). `s4_proposals/2026-05-29.json`.
2. **Industry by Type "Total"** — needs the Statement-1 Industry total fetched cross-statement (bespoke).
3. **Priority Sector "Total"** — no clean parent (40% of ANBC, overlapping categories); left without a Total.
4. **Source CSV in `web/public/`** — the 4.6 MB `atm_pos_consolidated.csv` still sits in the served dir (the
   compute scripts read it there); no longer fetched by the browser. Tidy: move source CSVs out of `public/`.
5. **3 SIBC status-substring warnings** (non-blocking fuzzy heuristic) — carry-forward.
6. **Parked forward bets** (`project_future_data_bets`, STRATEGY_PLANNER §9): per-bank granularity, ML
   forecasting.

---

## NEXT FOCUS — LinkedIn content for site engagement

The dashboard/engine is mature; the bottleneck is now **distribution**. Read these first:

- **[[project_distribution_reality]] (the key frame):** Risham's *reachable* LinkedIn network is mostly
  **tech / fintech PMs**, not bank-credit leadership. So **lead with the PAYMENTS pipeline** (UPI QR per POS,
  acquiring concentration, debit-card decline, Paytm-exit effects) framed for *builders* — it matches the warm
  graph far better than aggregate SIBC credit framed for risk officers. "Boring posts" is an audience-content
  mismatch, not a craft problem.
- **Workflow already built** — `analysis/newsletter/CLAUDE.md`: `generate_linkedin.py` produces a **7-post
  package** (1 anchor + 6 signal posts, .txt + .png) from `newsletter_config.json` + `signal_registry.json`.
  Post types (Milestone / Correction / Trend) + the short-form format + the per-cycle checklist are documented
  there. `generate_images.py` must run first (Mermaid → PNG).
- **Raw material is rich now** — the payments dashboard has 11 LLM-narrated, fully-traceable cards (YoY
  trajectories, streaks, QR-per-POS 137×, etc.) + the `/opportunities` feed. These are the hooks.
- **Caveats:** the newsletter/LinkedIn subsystem is the **one exception path** not yet standardised to the
  unified pipeline (per `analysis/newsletter/CLAUDE.md`) — run its scripts directly. Same traceability ethos
  applies: every number in a post must trace to a computed signal.
- **Strategic backdrop (parked, not active):** [[project_strategic_levers]] (fewer-bigger-low-touch;
  per-bank granularity as the keystone; describe→benchmark→decide ticket ladder).

**Suggested first move next session:** generate ideas/hooks for a payments-led LinkedIn series aimed at the
fintech-PM graph, using the live payments signals as the spine.

---

## Verify-state quick commands
```bash
python3 analysis/check_signal_freshness.py     # both pipelines fresh
python3 analysis/run_evals.py                  # SIBC gate + build (incl. Check 4f strict)
python3 analysis/validate_opportunity_traceability.py --strict
# data: SIBC 7 DB periods (csv_date Jan2024–Apr2026); ATM/POS 28 periods Jan2024–Apr2026
```
