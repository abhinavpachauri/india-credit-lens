# PLAN — NBFC Sectoral Deployment as pipeline #3

Written 2026-09-16 before any code; **updated 2026-09-22 against what was actually built.**
Supersedes the "next = NBFC ingest" line in `CLAUDE.local.md`; consumes `ARCHITECTURE.md`
§"Adding a pipeline".

> ### STATUS — phases 0–2 built, 3 commits, NOT pushed
>
> | | |
> |---|---|
> | **Done** | **0** `d4579f7` a pipeline declares itself · **1** `83986de` a compute module is a shape · **2** `d145694` the denominator rule + the NBFC source, specced |
> | **Next** | **Phase 3** — implement the denominator rule. First phase that changes numbers already on the dashboard |
> | **Decided** | D1 · D2 · D3 · D4 · D6 settled (§7). **D5 deferred by the user** until L1 is on screen |
> | **State** | 678 tests · both gates ALL STAGES PASSED · working tree clean · nothing pushed |
>
> Phases 0–2 were a **no-op refactor plus a spec**: not one number on the dashboard moved.
> Everything from phase 3 onward changes data.

---

## 0. Read this first — the whole thing in plain English

**What the source is.** Every month RBI publishes a short statement: *how much credit
India's NBFCs have lent, broken down by sector* — farming, industry, services, retail, other.
We have three of these releases. It is the same **shape** of data as the bank-credit (SIBC)
statement we already ingest: one number (rupees outstanding), sliced by sector.

**Why it matters, in one sentence.** The dashboard already says every month that bank credit
is *"steered toward NBFCs — 48.3% of the growth at 35.7% YoY"* — we can see banks pouring
money into NBFCs, **and we cannot say what the NBFCs did with it.** This source answers that.

**What you asked for, and where each ask stands:**

| your ask | where it lives | status |
|---|---|---|
| 1. Reuse as much as possible, then review the architecture | §2, §6 | **two reuse items built and proven**; the review is a *measurement* taken after phase 6 |
| 2. A careful signal spec for L1 / L2 / L3 | §3 | **written** (phase 2). L1 fully specced; L2 skeleton-only on purpose; L3 out of scope |
| 3. Model, then show, the bank→NBFC relationship | §5 | **designed, not built.** You deferred it until L1 is on screen |

**The one genuinely hard idea in this plan** — everything else is plumbing — is §3.2, the
*denominator rule*. In plain terms:

> When we say *"Retail took 21% of the new money"*, 21% **of what**? We always divided by
> "the parts we can see". That is correct only when the parts we can see ARE the whole thing.
> RBI often publishes a parent — say Services — and then names only *some* of its children.
> Divide by the named children and every share comes out inflated, while every individual
> number still traces back to a real stored value, so no existing check can see it.
> On NBFC Services the inflation is almost exactly **2×**.

**How it plays out in practice.** Phases 0–2 are done and changed nothing visible. Phase 3
fixes the denominator across both existing pipelines (ten of twelve cuts unaffected; two
corrected; priority-sector loses a reading it should never have had). Phases 4–6 ingest NBFC
and put it on screen at `/nbfc`. Then we stop and you look at it before anything
cross-source begins.

### 0.1 Vocabulary used throughout

The plan uses this project's own words. They are precise, not decorative:

| word | what it means here |
|---|---|
| **cut** | a parent plus the parts it breaks into — e.g. Services → Transport, Trade, Commercial Real Estate. The unit the dashboard renders as one table |
| **scan** | a signal computed for *every part of a cut at once*, rather than one number about one thing |
| **size / share / growth** | how big a part is (₹) · what fraction of its parent it is (%) · how fast it changed (% YoY) |
| **momentum** | how many **units** (₹ crore) a part actually added over 12 months — the quantity a growth rate throws away |
| **allocation** | of the new money that arrived, what share each part took. The family the denominator rule governs |
| **coverage** | (new) on an "of which" cut: what fraction of the parent the named parts actually add up to |
| **coherence** | do the parts agree? `|net| ÷ gross`. It picks *which sentence is true*, and never suppresses a number |
| **band** | the two standing sentences above each dashboard table — *speed* (Layer 1) and *mix* (Layer 2) |
| **branch 1 / 2 / 3** | the three denominator cases: parts sum to the parent · parts are a subset · no parent published |
| **additive** | the parts sum exactly to the parent. **Measured per cut per period, never declared by hand** |
| **L1 / L2 / L3** | computed from the CSV alone · the causal model on top · authored strategy, ~6-monthly |
| **the gate** | `core/gate.py` — the one command that runs every check for a pipeline and refuses to pass on drift |
| **freshness** | recompute every stored signal from the CSV and fail on any difference. How a refactor is proven a no-op |

---

## 1. What the source actually is (read, not assumed)

Three files in the repo root, one per monthly release:

| file | released | data month |
|---|---|---|
| `NBFCSDC07072026.xlsx` | 7 Jul 2026 | May 2026 |
| `NBFCSDCE07082026_AN.xlsx` | 7 Aug 2026 | Jun 2026 |
| `NBFCCREDIT07092026_A.xlsx` | 7 Sep 2026 | Jul 2026 |

One sheet (`Press Release`), 15 sector rows, ₹ crore. **Each release carries FIVE dated
columns, not one**: `M-24`, the prior-prior FY end, `M-12`, the prior FY end, `M`.

**So "three months of data" is really eleven dates**, and seven of them can compute a YoY
on day one:

```
2024-05-31  2024-06-30  2024-07-31                       level only (no 2023 in any file)
2025-03-31                                               level only (no 2024-03)
2025-05-31  2025-06-30  2025-07-31   ← YoY vs 2024
2026-03-31                           ← YoY vs 2025-03
2026-05-31  2026-06-30  2026-07-31   ← YoY vs 2025
```

This matters: it is the difference between shipping a dashboard dimension with a flat line
and shipping one with a real series. **SIBC's own lesson applies in reverse** — signals
exist per ingested PERIOD, and SIBC spent a separate session backfilling 11 → 24. NBFC
should register all eleven from the first run, with the four level-only dates registered
**level-only and saying so**, exactly as the SIBC 2024 backfill did.

### 1.1 The hierarchy — and the one thing that is different from SIBC

```
NBFC Credit                                    total, ₹59.89 L Cr @ Jul 2026, +14.94% YoY
├─ 1. Agriculture and Allied Activities        ₹0.80 L Cr   +17.97%
├─ 2. Industry                    of which     ₹22.13 L Cr   +7.38%
│   └─ 2.1 Infrastructure         of which     ₹20.16 L Cr   +6.24%
│       └─ 2.1.1 Power                         ₹14.36 L Cr   +6.89%
├─ 3. Services                    of which     ₹7.69 L Cr   +15.19%
│   ├─ 3.1 Transport Operators                 ₹1.79 L Cr   +10.65%
│   ├─ 3.2 Trade                               ₹1.16 L Cr   +14.65%
│   └─ 3.3 Commercial Real Estate              ₹1.03 L Cr   +22.34%
├─ 4. Retail Loans                of which     ₹26.06 L Cr   +21.43%
│   ├─ 4.1 Housing Loans*                      ₹8.55 L Cr   +11.87%
│   ├─ 4.2 Vehicle Loans                       ₹6.29 L Cr   +15.09%
│   ├─ 4.3 Loans against gold jewellery        ₹3.54 L Cr   +68.52%   ← the headline
│   └─ 4.4 Consumer Durables                   ₹0.75 L Cr   +51.46%
└─ 5. Other Loans**                            ₹3.21 L Cr   +19.69%
```

**Level 1 is an exact partition** — the five sectors sum to the total to the rupee at all
eleven dates (verified, diff 0.0). **Levels 2 and 3 are "of which" subsets**: Services'
three named children cover 52% of Services, Retail's four cover 73%, Industry's one covers
91%. Nothing declares the remainder.

That single fact drives most of §3, because the movement family's `alloc` divides by the
**sum of the parts**, and for an "of which" cut that denominator is not the cut's movement.
This is the identical defect already recorded as open on PSL in `CLAUDE.local.md`
("PSL's `weight`/`weight_now` divide by the sum of those same non-additive parts").
**NBFC is the case that forces it to be fixed properly rather than carried.**

### 1.2 Two gifts this source gives that neither existing pipeline has

1. **RBI publishes the YoY itself** (columns J and K). Our computed YoY can be gated
   against the source's own figure — free external ground truth, checked to 1e-4 by hand on
   the total (14.94207 published vs 14.94207 computed). No other pipeline has this. It
   becomes a gate stage, not a spot check.
2. **The press release states the headline in machine-readable text**, per the 2026-09-11
   delivery note — a second independent anchor for the extractor.

### 1.3 Three caveats that must be written into the spec, not discovered later

- **Sample, not census.** Footnote 2: Upper- and Middle-Layer NBFCs + HFCs, **~87% of
  total NBFC credit** per RTP 2024-25. Every cross-pipeline ratio in §5 is therefore a
  ratio against 87% of the book. It must be stated wherever the number is published — this
  is a `gap_` node in the system model, authored on day one, the same way
  `gap_psl_totals_methodology` carries PSL's non-additivity.
- **Revisions are real — and the first draft of this line was wrong.** It read "float noise,
  not restatement", based only on the total (`2026-03-31`: 5,775,989.3656 / .3760 / .3661,
  which *is* noise). **Housing Loans is not**: the May→June releases restate it by **₹108 Cr**
  at Mar 2025 and **₹242 Cr** at Mar 2026. Small, but a genuine restatement. Policy is
  SIBC's — **last release wins** — and the consolidator PRINTS every disagreement, because a
  revision nobody saw is indistinguishable from a parsing bug. (Found by building it, 2026-09-22.)
- **The store will have gaps until releases accumulate.** The prior period of `2025-05-31`
  is `2025-03-31` — two months, not one. Any rule reading `prev_value` (status rules,
  acceleration) would silently compare across a gap. **Make the gap loud**: a prior-period
  comparison computes only when the gap is one cadence step, else the row is absent with a
  declared reason. This is `feedback_failure_that_looks_like_absence` applied before the
  fact instead of after it.

---

## 2. Reuse — what NBFC inherits, and what had to be built

> **In plain terms.** A new source should mostly be a *description* — "here is my file, here
> is my shape" — not a new program. We measured how true that was. Most of the machinery is
> already generic. Four things were not, and two of them are now fixed.

NBFC inherits the gate, the cut tables (discovered from the registry), the state band
(derived), `table_rows`, every validator and the cadence vocabulary **for free**. The four
gaps, and what actually happened to each:

| gap | plan said | outcome |
|---|---|---|
| **pipeline ids hardcoded** | read from the manifest (D1) | ✅ **DONE** `d4579f7`. 21 sites, not the ~15 counted. Order is *declared*, not alphabetical |
| **compute module per pipeline** | make it pipeline-parameterised, don't copy 960 LOC | ✅ **DONE** `83986de`. Cheaper than feared — see §2.1 |
| **card generator per pipeline** | no third generator; table + band only | ⬜ **not needed for v1** (D2). The general fork stays open |
| **web adapter + route** | copy `SibcReadMode` (211 LOC) for v1 | ⬜ **phase 6.** Still a copy; generalising is item 4, deferred |

### 2.1 The compute module was a five-line problem, not a 960-line one — confirmed

`ARCHITECTURE.md` warned that if ingesting NBFC meant copying `compute/sibc.py`, **the copy
IS the finding**. It did not. That module bound itself to SIBC in exactly three
module-level constants — the CSV path, the timeline path, and one shared DataFrame cache.
Everything else was already generic: `statement` and `is_psl` have always been compute
*params with defaults*, and the seventeen methods never knew which pipeline they served.

**What shipped:** `compute/sibc.py` → **`compute/csv_sector.py`**. A module is a **shape**
(*one measure over a code hierarchy*), not a pipeline. A manifest declares the shape it is
(`compute_module`); the engine's two `if pipeline == "sibc"` ladders are gone.

**What turned out to be genuinely variable was two OPTIONAL columns, not a schema:**

- `scope_column` — SIBC publishes code `2.3` as *"Large"* in Statement 1 and *"Beverage and
  Tobacco"* in Statement 2, so a code alone does not identify a row. The **param key is the
  column name**, which is why all 159 existing signals needed no edit.
- `memo_flag_column` — the priority-sector lens, a second view over the same rupees.

The shared columns are deliberately **not** parameterised: they mean the same thing in every
such source, so per-source spellings would invent a difference rather than describe one.
They are declared in `schema` and **verified against the file on load**, because a
declaration nothing checks decays into decoration — which is how one path came to be
hardcoded in nine modules.

**Proof it was a no-op:** freshness recomputed **17,809 SIBC + 151,448 payments rows
identically**, both gates passed, and zero derived artifacts changed.

### 2.2 ⚠️ What phases 0–2 changed about the phases still to come

Three contracts now exist that the original plan could not know about. **Phase 4 must honour
them or it will not load.**

1. **The consolidated CSV must carry exactly these column names** — verified on load,
   `ValueError` otherwise:
   ```
   date · code · parent_code · level · sector · outstanding_cr
   ```
   NBFC has no `statement` and no memo flag, so its `schema` block simply omits
   `scope_column` / `memo_flag_column`. Declaring a column the file lacks now *raises*.
2. **The manifest must declare `compute_module: "csv_sector"`** and an `order` (3). An
   undeclared or unknown `compute_module` raises rather than computing nothing silently.
3. **Tests and guards use `manifest.pipelines_with_stage(...)`**, not the pipeline list. So a
   pipeline that legitimately produces no planes sidecar is never asked for one — which is
   exactly NBFC's case under D2 (no cards ⇒ no planes). **Phase 5 must not add a
   `stamp_planes` stage** to the NBFC manifest just for symmetry.

---

## 3. Signal spec (the part to get right before any compute runs)

> **In plain terms.** A "signal" is one measured fact the platform stores and can be held to
> — *industry credit is ₹48 lakh crore*, *it grew 20.0% YoY*, *it took 33.8% of the new
> money*. Specifying them first matters because a signal is a promise: once stored, prose
> quotes it and a gate checks the prose against it. Writing the spec before the compute is
> the project's standing rule, and §3.2 is the case for it — two real problems only appeared
> because the rule had to be written down as sentences.

`analysis/signals/README.md` gains an NBFC section; the registry gains the entries below.
**Nothing is computed until the spec is written**, per the standing spec-first rule.

### 3.1 Layer 1 — what the cuts are

Five cuts. Naming follows `one cut, one name` (`nbfc-{slug}-{family}`):

| stem | subject | parts | additive? |
|---|---|---|---|
| `nbfc-main` | NBFC Credit | the 5 level-1 sectors | **yes** (exact) |
| `nbfc-industry` | Industry | Infrastructure | no — "of which" |
| `nbfc-infra` | Infrastructure | Power | no — "of which" |
| `nbfc-services` | Services | Transport / Trade / CRE | no — "of which" |
| `nbfc-retail` | Retail Loans | Housing / Vehicle / Gold / Consumer Durables | no — "of which" |

Per cut, the families §17–§20 made the generic L1 surface — **size, share-of-parent,
share-of-book, growth, momentum, acceleration, allocation** — plus the cut's own parent
rate. Every entry declares `cadence: "monthly"` and `window: 12` (both now required
fields; Check 2e fails a signal without cadence).

Plus scalars the prose layer needs: `nbfc-credit-abs`, `nbfc-credit-yoy`,
`nbfc-retail-yoy`, `nbfc-gold-yoy`, and the FY-end pair (`2025-03-31` / `2026-03-31` are
both in the store, so `csv_sector_fy_delta` works from day one).

Estimated registry growth: **~55 entries**, 527 → ~582.

### 3.2 The denominator rule — MEASURED per cut, never declared (D3, settled)

**What `allocation` is.** For a cut it answers *"of the new money that went into this book
over 12 months, what share did each part take?"* It needs a denominator: the total movement
of the thing being decomposed. Today the compute uses **the sum of the parts' movements**,
which is right exactly when the parts ARE the whole.

**The rule (user's, 2026-09-16): compute whether they are. Don't declare it.** A
hand-written `additive: true|false` field is another `MOVEMENT_CUTS` — a second description
of the data that agrees today and drifts later. Instead, per cut per period:

```
        Σ parts  ==  parent's published row  ?
              │                        │
             yes                       no ────────── parent row published?
              │                                   │              │
    denominator = Σ parts                        yes             no
    (identical to the parent —                    │              │
     use either)                    denominator = parent's    WITHHOLD
                                    published net, and         and declare
                                    state the coverage         the reason
```

One logic, every cut, both pipelines.

### 3.2.1 What it measures — enumerated over all 12 SIBC cuts × all 24 dates

Not sampled. The separation is absolute, so no threshold needs tuning:

```
ADDITIVE cuts — worst residual across every cut, every date: 0.000000%
NON-additive:  sibc-main       4.5% – 5.6%   (24 of 24 dates)
               sibc-nbfc-sub  63.9% – 67.2%  (24 of 24 dates)
```

Ten of twelve cuts are **exactly** additive — the rule is a provable no-op there. Two change,
and the movement gap (what allocation actually divides by) at 2026-07-31 is:

| cut | parent net | Σ parts net | today's shares are | |
|---|---|---|---|---|
| `sibc-main` | +3,515,561 | +3,351,049 | **×1.049 too high** | RBI attributes ₹10.7 L Cr to no sector |
| `sibc-nbfc-sub` | +559,147 | +223,995 | **×2.49 too high** | HFCs + PFIs are 34.7% of bank credit to NBFCs |
| `sibc-psl` | *none published* | — | **withheld** | overlapping memo lenses, §3.2.2 |
| the other 9 | — | — | unchanged | |

**The most-broken cut today is `sibc-nbfc-sub`** — the exact entity this new pipeline hangs
off. A card there saying "HFCs took X% of new NBFC credit" is overstating by a factor of
2.5, because two named children cover a third of the parent. Fixing it is a prerequisite
for the §5 cross-source work being trustworthy, not a side quest.

⚠️ **The trap, and it is the `feedback_check_population` shape again.** `sibc-industry-type`
*looks* parentless: its parts are Statement 2, its total is Statement 1, and **Statement 2
carries no code "2" at all**. Its 19 parts sum to ₹4,800,777 — the published Industry total,
**exactly**. So it is branch 1, and a naive implementation would misread "I cannot find the
parent" as "there is no parent" and withhold a correct allocation. Therefore: **a parent
code that is declared but not found must RAISE, never silently withhold.** The share scan
already solved this with `parent_statement`; momentum and allocation must declare it too.

### 3.2.2 PSL is the withhold branch — and its invented denominator is live today

**RBI publishes no PSL total.** PSL's ten lines are overlapping lenses over the main tree,
not a partition: PSL "Micro and Small Enterprises" (₹30.88 L Cr) spans industry *and*
services and is not the Statement-1 ₹10.92 L Cr row; a weaker-section loan to an MSE
borrower is counted in **both** that line and "Weaker Sections" (₹21.22 L Cr). Their sum is
not a book, so a share of it is not a share of anything.

The dashboard says both things at once, in one block:

```
no_speed_note : "Priority sector is a memo lens over the main tree, not a slice of it,
                 so RBI publishes no total for it to grow at."
mix           : "Drifting toward Micro and Small Enterprises — it took 37.2% of the
                 growth while holding 30.0% of the total a year ago."
```

The second sentence divides by the total the first says does not exist. Under the rule PSL
lands in the withhold branch: `alloc`, `weight`, `weight_now` go, the derived mix state with
them, and the band's mix row carries its declared reason — the designed behaviour for a
reading that cannot exist. Size, share-of-book, growth and momentum are untouched.
Precedent is already set: `share-of-parent` was deleted for PSL rather than given an
invented total (`gap_psl_totals_methodology`).

### 3.2.3 What it means for NBFC

`nbfc-main` is branch 1 (the five sectors sum to the total to the rupee at all eleven
dates). The four "of which" cuts are branch 2 — the parent row is always published, so the
correct denominator always exists. Worked example, NBFC Services Jul 2025 → Jul 2026:

```
                       over Σ PARTS   over PARENT's net
Transport Operators          33.8%           17.0%
Trade                        29.0%           14.6%
Commercial Real Estate       37.2%           18.6%
unnamed remainder               —            49.8%
```

×1.99 across the board: the card would read *"CRE took 37% of all new NBFC services
credit"* when it took **18.6%**, because RBI names 51.7% of that cut and half the new money
went somewhere it does not break out.

### 3.3 Layer 2

- **Skeleton only, on day one.** `generate_skeleton.py` is deterministic and gives the 15
  entity nodes, the structural edges, URNs and `concept_tags` for free. Mix states fall out
  of the movement rows with no new registry entries (that mechanism is already keyed on
  `compute.parent_code`), so the state band lights up on all five cuts with zero authoring.
- **Behavioral layer (forces/channels) DEFERRED.** A FOUNDATION pass wants a series long
  enough to argue over; eleven dates with two-month gaps is not it, and a force authored
  now would be sourced against a window we cannot yet measure. Revisit at ~6 releases
  (≈ Feb 2027), or at the first FY-end file. **`_meta.update_note` records the deferral
  explicitly** so the absence is declared rather than looking like a miss.
- The one exception worth authoring immediately is the `gap_` node for the 87% sample
  (§1.3) — it is a fact about the source, not a causal claim, and everything downstream
  needs to be able to cite it.

### 3.4 Layer 3

Out of scope. It consumes L2a/L2b causal graphs, and NBFC has none yet.

---

## 4. Ingestion — stages, and what is genuinely new

> **In plain terms.** Ingestion is four moves: check the file still looks the way we expect,
> pull the numbers out, append them to one consolidated CSV, and register the period. NBFC
> is *simpler* than SIBC here — it loses a stage SIBC needs and gains one no other pipeline
> has.

Mirrors SIBC, minus one stage and plus one:

| stage | module | note |
|---|---|---|
| 0 detect_format | `pipelines/nbfc/detect_format.py` | header row, five date columns, 15 labels, footnote text |
| 0.6 extract | `pipelines/nbfc/extract_nbfc.py` | sheet → `sections.json`; **archives the XLSX to `{period}/raw/`** (the omission that made SIBC's ingest a silent no-op) |
| 0.7 consolidate | `pipelines/nbfc/consolidate_nbfc.py` | all five columns → CSV, upsert by `(date, code)`, last release wins |
| ~~1a date remap~~ | — | **does not exist.** The column header IS the data date; there is no fortnightly publication ambiguity to resolve. `period == csv_date`; the release date is metadata |
| 1b csv integrity | builtin | |
| **1c published-YoY check** | `pipelines/nbfc/validate_published_yoy.py` | **new, and unique to this source** — our computed YoY vs RBI's own printed column, hard fail outside 1e-4 |
| 2e/2f/… | core, unchanged | inherited |
| 5.9/5.9b/5.9c/5.9d | core, unchanged | band + table + both traceability gates, inherited |

**`timeline.json`** registers the release with its data date, and each backfilled date with
`"backfilled_from": "<release>"` so the provenance of a date nobody released is visible.

**No Stage 5 evaluate, no S4, no narrative** in v1 (see D2) — therefore **zero API spend**
for the whole ingestion. Stated explicitly because the standing rule
(`feedback_never_spend_api_unasked`) means any paid call needs approval in the
conversation, and this plan deliberately needs none.

---

## 5. The SIBC ↔ NBFC relationship (the third ask)

> **In plain terms.** Banks lend to NBFCs; NBFCs lend to everyone else. So there are two
> completely different questions, and mixing them would be the worst error available here:
>
> **(a) Vertical — the pipe.** How much of the NBFC book do banks fund? A ratio, and a
> sanity check: if it lurches, a definition broke, not the economy.
>
> **(b) Horizontal — the same borrower, two lenders.** Both statements name Power, Vehicle
> Loans, Gold, Housing, Consumer Durables. So we can finally say *who is actually lending to
> that sector* — and NBFCs turn out to be the **majority** lender in power (62.3%) and
> consumer durables (76.4%).
>
> **What we must never publish:** bank credit + NBFC credit as a "total". Bank credit to
> NBFCs *funds* the NBFC book, so adding them double-counts. The registry already carries a
> signal saying so.

This is the strongest cross-source pairing the platform has had. SIBC↔payments shares a
handful of concepts; **SIBC and NBFC share a taxonomy.**

### 5.1 Two distinct relationships — they must not be conflated

**(a) The vertical one — funding → deployment.** SIBC's `3.9 Non-Banking Financial
Companies` (₹21.28 L Cr @ Jul 2026) is banks' *claim on* NBFCs. The NBFC pipeline is what
NBFCs did with their money. Bank credit is **35.5%** of the NBFC book — a funding-dependence
proxy, not an identity (bonds, ECB, deposits and equity fund the rest, and the book is the
87% sample). Modelled as a **reconciliation constraint** with a plausibility corridor,
exactly like `cx_cc_balance_per_card`: a move outside the corridor means a definition or
roster break, not an economic event.

**(b) The horizontal one — the same borrower, two channels.** Eight sectors appear in both
statements. This is where the analysis actually lives:

```
sector                bank ₹Cr     NBFC ₹Cr    NBFC share of the two
Power                   869,333   1,436,271          62.3%    ← NBFCs are the majority lender
Consumer Durables        23,027      74,644          76.4%    ← banks have almost left
Vehicle Loans           764,554     629,239          45.1%
Gold jewellery          552,261     353,988          39.1%    ← NBFC side +68.5% YoY
Transport Operators     290,424     178,772          38.1%
Housing                3,428,453    854,627          20.0%
Commercial Real Est.    669,260     103,485          13.4%
Trade                 1,408,848     115,516           7.6%
```

The platform has never been able to say *"banks hold 38% of India's power credit"*. It can
now — and the same eight rows answer "where are banks retreating and NBFCs advancing",
which is a genuine credit-cycle read, not a restatement.

### 5.2 How it is modelled — reusing the hub, adding nothing new

Everything needed exists in `COMPOSITION_SPEC` Part II:

1. **`ontology/concepts.json`** gains the shared concept tags (`power`, `vehicle_loan`,
   `gold_loan`, `consumer_durable`, `housing`, `cre`, `transport`, `trade`). NBFC entities
   carry URNs (`icl:nbfc/2.1.1`) and those tags; SIBC entities already carry most.
2. **`derive_cross_links.py`** then proposes the eight pairs on the existing shared-concept
   rule, with **no new derivation mechanism** — they land in `cross_source/candidates.json`
   for confirmation into `composition.json`. *If it does not propose them, the finding is in
   the deriver, and that is worth knowing.*
3. **One construct per shared sector** (`system_credit_to_power`), members = the two
   entities, direction + **coherence** computed by the existing `construct_direction`. The
   interesting state is the *contested* one: banks shrinking while NBFCs grow is precisely
   what coherence was built to route.
4. **One reconciliation constraint** `cx_nbfc_bank_funding_share` for 5.1(a), corridor
   authored wide (say 25–50%) and **negative-tested**, like `cx_cc_balance_per_card`.
5. **One cross-pipeline loop** (deferred with the rest of the behavioral layer): bank credit
   to NBFCs → NBFC deployment → asset growth → further bank funding.

### 5.3 How it reaches a reader — UI

⚠️ **CLAUDE.md authoring rule: ASCII layout first, explicit approval, then code.** So this
section states the *shape* and nothing is built until an ASCII pass is approved separately.

- **v1, no new surface.** The eight pairs are cross-system items; the read mode already
  renders cross-system findings inline (`crossSystemFor()`), matched to a dimension. A
  "banks hold 38% of power credit" card attaches to the SIBC Industry dimension **and** the
  NBFC Industry dimension from one computed item.
- **v2, the surface this data earns — a channel column.** The §17 table is per-cut rows ×
  declared columns. A shared-sector cut could carry `bank ₹ | NBFC ₹ | NBFC share | Δ share`
  using `table_rows` unchanged, because a column is declared per cut. That is the cheapest
  possible route to the finding and it reuses the generic surface rather than adding one.
- **Deliberately NOT v1:** a combined "system credit" total. Adding bank and NBFC books
  double-counts — bank credit to NBFCs funds the NBFC book. Publishing a sum would be
  wrong, and `nbfc-double-counting` is already a registry entry saying so.

---

## 6. Phases, in dependency order

> **In plain terms.** Phases 0–2 tidied the machinery and wrote down the rules; nothing a
> reader could see changed. Phase 3 corrects arithmetic that is currently wrong on the live
> dashboard. Phases 4–6 bring NBFC in and put it on screen. Then we stop.

| # | phase | output | status |
|---|---|---|---|
| **0** | pipeline ids from the manifest | one constant, 21 call sites | ✅ **`d4579f7`** — 672 tests, both gates green, **0 derived artifacts changed** |
| **1** | `compute/sibc.py` → `compute/csv_sector.py`, shape-not-pipeline | no behaviour change | ✅ **`83986de`** — **17,809 + 151,448 rows recompute identically**; 678 tests |
| **2** | the denominator rule + the NBFC source, specced in `signals/README.md` | spec only | ✅ **`d145694`** — no compute, no registry entries; reconcile clean |
| **3** | **implement** the measured denominator rule | one logic, all cuts, both pipelines | ⬜ **NEXT.** 10 of 12 cuts byte-identical; `sibc-main` + `sibc-nbfc-sub` corrected; `sibc-psl` withheld |
| **4** | extractor + consolidator + `pipeline.json` + timeline | `nbfc_consolidated.csv` — 15 sectors × 11 dates = **165 rows** | ⬜ must honour §2.2 |
| **5** | **registry entries** + L1 append (11 dates) + skeleton + band + cut tables | registry + signals.db + sidecars | ⬜ 2e/2f/5.9b/5.9d green |
| **6** | web adapter + `/nbfc` route | a third dashboard: table + band, no cards | ⬜ tsc + build; verify 1440 and 375 |
| ⛔ | **STOP — you review Layer 1 on screen** | — | the user's instruction, 2026-09-16 |
| **7** | concepts, URNs, cross-links, construct + constraint (§5.2) | cross-source items | ⬜ deferred (D5) |
| **8** | UI for the channel view — **ASCII approval first** (§5.3) | per D5 | ⬜ deferred (D5) |

**Phases 0–1 were pure refactor, provable by freshness, and worth doing even if NBFC were
cancelled.** They closed items 1 and 2 of `ARCHITECTURE.md`'s "Adding a pipeline" list.

### 6.1 What to expect from phase 3 — it is the noisy one

Every phase so far has been provable by *nothing changing*. Phase 3 is the first that
**deliberately changes stored values**, so the usual proof inverts:

- **Freshness will report drift, and that is the point.** signals.db must be re-appended
  across every period afterwards; a green freshness run *before* re-appending would mean the
  fix did not take.
- **Priority Sector loses its mix state.** Its band row falls back to a declared reason —
  the designed behaviour for a reading that cannot exist. Size, share-of-book, growth and
  momentum are untouched.
- **Two cuts change their published shares**: `sibc-main` (÷1.049) and `sibc-nbfc-sub`
  (÷2.49). Ten others are byte-identical, which is the regression test.
- **A new stored row appears** — `coverage` — so prose can say "the named parts are 51.7%
  of this cut" instead of implying the cut is fully decomposed.
- **`contribution` and `coherence` must be relabelled on branch-2 cuts**, not just
  redenominated. They stay over the named parts because the unnamed remainder's *gross* is
  unknowable, so the sentence becomes "of the movement among the named parts".

### After the ingest — the architecture review the request asks for

The honest test of `ARCHITECTURE.md`'s claim is a count taken *after* phase 6, not before:
**how many files did a third pipeline touch that were not its own?** Record it in
`ARCHITECTURE.md` next to the 2026-09-16 measurement, and let the delta decide whether
items 3 and 4 (generic card path, pipeline-agnostic web adapter) get built or stay deferred.
That is the review, and it is a measurement rather than an opinion.

---

## 7. Decisions (D1–D6) — five settled, one deferred

| | decision | recommendation |
|---|---|---|
| **D1** | ~~`PIPELINE_IDS` from the manifest?~~ | **SETTLED + BUILT `d4579f7`** — 21 sites, not 15. Item 1 of the architecture list closed |
| **D2** | ~~Card generator for NBFC?~~ | **SETTLED — table + band for v1, cards deferred not refused.** 11 dates is a thin basis for "fastest in N periods". Revisit at ~12 releases |
| **D3** | ~~Denominator rule?~~ | **SETTLED — measure it per cut, one logic everywhere** (§3.2). Measured: 10 of 12 SIBC cuts unaffected, 2 corrected, PSL withheld |
| **D4** | ~~Route?~~ | **SETTLED — `/nbfc`, its own path.** A different lender population; folding it into bank credit invites the double-count §5.3 bans |
| **D5** | Cross-source UI — the second-order view | **DEFERRED BY THE USER until L1 is processed and reviewed.** Phases 7–8 do not start until then |
| **D6** | ~~Register all 11 dates?~~ | **SETTLED 2026-09-16 — all 11.** |

## 8. Risks

- ~~**Phase 1 is the one that can bite.**~~ **RETIRED 2026-09-22 — it did not.** Freshness
  recomputed 169,257 rows identically and both gates passed. The mitigation worked exactly as
  designed, which is the argument for keeping that guard un-fingerprinted.
- **Phase 3 is now the one that can bite, for the opposite reason.** It is the first phase
  that *should* change stored values, so "nothing changed" stops being the proof and becomes
  the failure mode: a green freshness run before re-appending means the fix did not take.
  The regression test is that the **ten unaffected cuts stay byte-identical** while the two
  corrected ones move by the predicted factors (§6.1).
- **Format drift.** Three files, one format. The detector is being written against n=3; the
  fourth release is the real test. Keep stage 0 loud rather than tolerant.
- **The 87% sample invites a wrong sentence** ("NBFC credit is ₹59.9 L Cr" — it is not,
  that is 87% of it). This is a prose risk, so it belongs to gate 5.8 and the `gap_` node,
  not to a compute check.
- **Eight cross-links are eight chances to pair the wrong entities.** Bank "Housing" is
  `4.2 Housing (incl. Priority Sector Housing)`; NBFC "Housing Loans*" includes HFC lending,
  and HFCs also appear inside SIBC as `3.9.1`. Every pair gets a written note on what the
  two sides do and do not cover, or it does not get confirmed.
